import { HeaderType } from "./variants";

export const isSlim = (headerType: HeaderType) => "slim" === headerType;
export const isPresentation = (headerType: HeaderType) => "presentation" === headerType;
